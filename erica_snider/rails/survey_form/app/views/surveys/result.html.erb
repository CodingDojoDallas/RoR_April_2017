<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Survey Form</title>
  </head>
  <body>
    <% if flash.key?(:errors) %>
      <% if flash[:errors].key?("name") %>
        <% flash[:errors]["name"].each do |error_messages| %>
          <p><%= error_messages %></p>
        <% end %>
      <% end %>
    <% else %>
      <p>Thank you for submitting the form. You have submitted this form <%= session[:count] %> times now.</p>
    <% end %>

    <h3>Submitted Information</h3>
    <p>Name: <%= session[:survey]["name"] %></p>
    <p>Dojo Location: <%= session[:survey]["location"] %></p>
    <p>Favorite Language: <%= session[:survey]["language"] %></p>
    <p>Comment: <%= session[:survey]["comment"] %></p>

    <br><br><br>

    <form action="/" method="post">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <input type="submit" value="Go Back">
    </form>
  </body>
</html>
